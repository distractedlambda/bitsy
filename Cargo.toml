[package]
name = "bitsy"
version = "0.1.0"
edition = "2021"

[dependencies.rand]
version = "0.8.5"
features = ["nightly"]

[profile.release]
opt-level = 3
overflow-checks = false
lto = "fat"
codegen-units = 1
